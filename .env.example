# DeepSeek AI 配置（仅首次启动时用于初始化 models.json 的默认模型）
# 之后模型配置通过界面"模型管理"添加和切换，修改此文件不再生效
DEEPSEEK_API_KEY=
DEEPSEEK_API_URL=https://api.deepseek.com/v1

# 默认SSH连接配置（仅在 servers.json 不存在时用于初始化）
SSH_HOST=
SSH_USER=
SSH_PASSWORD=
SSH_PORT=
